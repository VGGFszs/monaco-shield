{
  "name": "monaco-shield",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:all": "concurrently -n onboarding,dashboard,web,api -c blue,green,magenta,cyan \"npm run dev --workspace=@monaco-shield/onboarding\" \"npm run dev --workspace=@monaco-shield/dashboard\" \"npm run dev --workspace=@monaco-shield/web\" \"npm run dev --workspace=@monaco-shield/api\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/jest": "^29.5.14",
    "@types/supertest": "^6.0.3",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "playwright": "^1.52.0",
    "supertest": "^7.1.0",
    "ts-jest": "^29.3.2",
    "turbo": "^1.12.4"
  },
  "packageManager": "npm@10.9.2"
}
